#include <stdio.h>
#include <string.h>

void Reverse (int size){
     char x;
     scanf("%d", &x);

 for (i=0; i<size; i++){
 Reverse (size);

 }
  printf("%c", c);
}

int main(){
    int size;
printf("Enter the size of the array\t");
scanf ("%d", &size);
printf("Enter the array\t");
Reverse(size); 
return 0; 
}           